# See contributing/js-dependencies.md.
# Install scripts never run: a package that needs one is a reason to pick another.
ignore-scripts=true
# Exact versions in package.json; updates are deliberate diffs.
save-exact=true
