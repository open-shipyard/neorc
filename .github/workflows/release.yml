name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package: [neorc-core, neorc-ui, neorc]
    steps:
      - uses: actions/checkout@v7
        with:
          # hatch-vcs derives every package's version from the git tag.
          fetch-depth: 0
          persist-credentials: false
      - uses: astral-sh/setup-uv@v4
      # Building neorc-ui builds the web UI from ts/neorc-ui, with Node.js at
      # the version in its .nvmrc and the committed lockfile: the only place a
      # released bundle is ever built.
      - uses: actions/setup-node@v7
        with:
          node-version-file: ts/neorc-ui/.nvmrc
          cache: npm
          cache-dependency-path: ts/neorc-ui/package-lock.json
      - run: uv build --package ${{ matrix.package }} --out-dir dist
      - uses: actions/upload-artifact@v7
        with:
          name: dist-${{ matrix.package }}
          path: dist/

  publish-pypi:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      # PyPI Trusted Publishing mints one token per project, so each package is
      # uploaded on its own. Serialized so neorc-core and neorc-ui land before
      # neorc, which depends on them.
      max-parallel: 1
      matrix:
        package: [neorc-core, neorc-ui, neorc]
    environment:
      name: pypi
      url: https://pypi.org/project/${{ matrix.package }}/
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: dist-${{ matrix.package }}
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1

  github-release:
    needs: publish-pypi
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: dist-*
          merge-multiple: true
          path: dist/
      - run: >-
          gh release create "$GITHUB_REF_NAME" dist/*
          --repo "$GITHUB_REPOSITORY"
          --title "$GITHUB_REF_NAME"
          --verify-tag
          --generate-notes
        env:
          GH_TOKEN: ${{ github.token }}
